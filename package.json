{
  "name": "pruebas-ls",
  "version": "1.1.3",
  "description": "Command line client for local-stamp",
  "bin": {
    "nlocal-stamp.sh": "bin/nlocal-stamp.sh"
  },
  "scripts": {
    "install": "./bin/saasdk-install.sh; npm install -g @kumori/cli"
  }
}
